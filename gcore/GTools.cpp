#include "GTools.h"
#include "../gobjects/GLine.h"

void GTools::add_line(size_t _xp1, size_t _yp1, size_t _xp2, size_t _yp2)
{
    // create new line object with first point.
    // GLine* line_ptr = new GLine(GVertex{_xp1, _yp1});

    // add second point to line.
    // line_ptr->add_vertex(GVertex{_xp2, _yp2});

    // add line to model of current page.
    // m_CurrentPage->model()->add_entity(line_ptr);
}
